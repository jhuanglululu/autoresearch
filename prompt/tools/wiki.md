Read and search the shared research wiki (read-only; writes go through wiki_write).

Actions: search (dual-path semantic + lexical; heed the divergence warning when the
two disagree), read (note + its evidence footer and backlinks), graph (query typed
relations: extends / combines / refutes), list.

(Skeleton — port the full description from research-bot's prompt/tools/wiki.md and
prompt/tools/source.md: "Only sources are evidence. A summary citing another summary
is navigation, not support.")

Show one summary's typed relations in the idea graph — its outgoing and incoming edges.
Read-only. Requires `slug`; pass an optional `relation` (extends / combines / refutes) to
filter to a single relation type.

Relations are navigation, not evidence: they connect ideas, they do not verify them.

Slug format: lowercase with hyphens, no spaces (e.g. "rope-scaling").

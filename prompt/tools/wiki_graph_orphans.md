List summaries with no typed relation in or out — the ideas nobody has connected to the
rest of the graph yet. Read-only.

Optional `type` (default "idea") selects which notes to scan; note types are paper /
mechanism / idea / experiment / result, or "any" for all of them.
